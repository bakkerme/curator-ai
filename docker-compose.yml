services:
  curator:
    image: curator-ai:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    environment:
      CURATOR_CONFIG: /app/curator.yaml

    volumes:
      - ./curator.yaml:/app/curator.yaml:ro
    restart: unless-stopped